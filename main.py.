
import streamlit as st
import plotly.graph_objects as go

# 1. إعداد الصفحة
st.set_page_config(page_title="VesselCore Technical", layout="wide")

# 2. الأسماء الحقيقية للسفن
fleet = ["NJ MOON", "NJ MARS", "NJ AIO", "YARA J"]

# 3. القائمة الجانبية
st.sidebar.title("🚢 VesselCore")
st.sidebar.write("المدير الفني: مروان كروم")
selected_ship = st.sidebar.selectbox("اختر السفينة:", fleet, key="vessel_select")

# 4. المحتوى الرئيسي
st.title(f"لوحة التحكم: {selected_ship}")
st.write(f"تحليل الأداء الفني لسفينة {selected_ship} تتبع شركة NJ TRUST MARINE")

# 5. رسم بياني بسيط للتجربة
fig = go.Figure(data=[go.Bar(x=fleet, y=[85, 82, 90, 88])])
fig.update_layout(title="حمل المحرك الحالي (%)")
st.plotly_chart(fig, use_container_width=True)

st.success("النظام يعمل الآن بنجاح.")
